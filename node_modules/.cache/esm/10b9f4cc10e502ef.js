let keyInSelect,question,detect,chalk;_10e‍.x([["sprachErkennung",()=>sprachErkennung],["farbeAendern",()=>farbeAendern]]);_10e‍.w("readline-sync",[["keyInSelect",["keyInSelect"],function(v){keyInSelect=v}],["question",["question"],function(v){question=v}]]);_10e‍.w("langdetect",[["detect",["detect"],function(v){detect=v}]]);_10e‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);



//gibt die benutzte Sprache aus z.B. en usw.
       const sprachErkennung = () => {
  const sprache = readlineSync.question("Gib einen Text ein: ");
  const result = detect(sprache);
  _10e‍.g.console.log(result[0].lang);
};

//Auswahl um die Farbe vom Text zu ändern
       const farbeAendern = () => {
  const text = question("Gib einen Text ein: ");

  const colors = ["Rot", "Gruen", "Blau", "Gelb"];
  const index = keyInSelect(colors, "Waehle eine Farbe aus: ");

  if (index !== -1) {
    let coloredText = "";
    switch (index) {
      case 0:
        coloredText = chalk.red(text);
        break;
      case 1:
        coloredText = chalk.green(text);
        break;
      case 2:
        coloredText = chalk.blue(text);
        break;
      case 3:
        coloredText = chalk.yellow(text);
        break;
      default:
        coloredText = text;
    }

    _10e‍.g.console.log("Farbiger Text:", coloredText);
  } else {
    console.log("Keine Farbe ausgewählt.");
  }
};

// export const funktion3 = () =>{

// };

// export const funktion4 = () =>{

// };
