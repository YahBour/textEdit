let keyInSelect,question,detect,chalk;_8ed‍.x([["sprachErkennung",()=>sprachErkennung],["farbeAendern",()=>farbeAendern]]);_8ed‍.w("readline-sync",[["keyInSelect",["keyInSelect"],function(v){keyInSelect=v}],["question",["question"],function(v){question=v}]]);_8ed‍.w("langdetect",[["detect",["detect"],function(v){detect=v}]]);_8ed‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);



//gibt die benutzte Sprache aus z.B. en usw.
       const sprachErkennung = () => {
  const sprache = question("Gib einen Text ein: ");
  const result = detect(sprache);
  _8ed‍.g.console.log(result[0].lang);
};

// Auswahl um die Farbe vom Text zu ändern

       const farbeAendern = () => {
  const text = question("Gib einen Text ein: ");

  const colors = ["Rot", "Gruen", "Blau", "Gelb"];
  const index = keyInSelect(colors, "Waehle eine Farbe aus: ");

  const result = detect(text);

  if (index !== -1) {
    let coloredText = "";
    switch (index) {
      case 0:
        coloredText = chalk.red(text);
        break;
      case 1:
        coloredText = chalk.green(text);
        break;
      case 2:
        coloredText = chalk.blue(text);
        break;
      case 3:
        coloredText = chalk.yellow(text);
        break;
      default:
        coloredText = text;
    }

    _8ed‍.g.console.log("Farbiger Text:", coloredText);
  } else {
    console.log("Keine Farbe ausgewählt.");
  }
  _8ed‍.g.console.log(
    "der Text wurde in der Sprache: " + result[0].lang + " geschrieben"
  );
};

// export const funktion3 = () =>{

// };

// export const funktion4 = () =>{

// };
